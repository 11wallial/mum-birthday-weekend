GdUnit4 is not vendored. Install it before running the suite:

  gdunit4 as an addon → https://github.com/MikeSchulze/gdUnit4
  unzip into addons/gdUnit4/ and enable it in Project Settings → Plugins

CI installs it the same way; see .github/workflows/godot.yml at the repo root.

Room dressing per floor (carpet, wiring, cash stacks, rule boards). Model each
piece so it can be added to a room incrementally — the room is meant to
accumulate over a run rather than swap wholesale between floors.

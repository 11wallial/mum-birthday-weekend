[gd_resource type="Theme" load_steps=2 format=3]

; The project's default face. Every Control that does not ask for another
; one gets IBM Plex Sans through this; UiSkin and Type name the exceptions.

[ext_resource type="FontFile" path="res://assets/fonts/IBMPlexSans.ttf" id="1_body"]

[resource]
default_font = ExtResource("1_body")
default_font_size = 15

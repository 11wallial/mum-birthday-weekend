[gd_resource type="Resource" script_class="UnlockDef" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/resources/unlock_def.gd" id="1_unlockdef"]

[resource]
script = ExtResource("1_unlockdef")
id = &"unlock_challenge_interest_only"
display_name = "Interest Only"
description = "A challenge run: every floor's debt interest is forty points higher."
kind = 3
target_id = &"interest_only"
condition = 4
threshold = 100
